//: [Previous](@previous)

/*: # Challenge
 Do you feel confident about a challenge?
 
 1. Create a custom `Shape`;
 2. Create a custom transition (careful, not a SwiftUI.Transition) between list and details in our application in which the poster from the list expands to the detail page. Hint: `.matchedGeometryEffect(_:)`.
 */
